# PostgreSQL
POSTGRES_USER=lava
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=lava_rapido

# Keycloak admin (dev only)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change-me-in-production

# MinIO
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=change-me-in-production

# API (when added)
API_PORT=3001
DATABASE_URL=postgresql://lava:change-me-in-production@postgres:5432/lava_rapido
REDIS_URL=redis://redis:6379
MINIO_ENDPOINT=minio:9000
KEYCLOAK_ISSUER=http://localhost:8080/realms/lava-rapido
